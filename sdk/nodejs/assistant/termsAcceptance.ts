// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages Grafana Assistant terms acceptance for the stack configured by the provider. Setting `accepted` to `true` constitutes acceptance of the current Assistant terms for the stack. The authenticated identity must have the `grafana-assistant-app.settings.terms:write` permission.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const example = new grafana.assistant.TermsAcceptance("example", {accepted: true});
 * ```
 *
 * ## Import
 *
 * ```sh
 * terraform import grafana_assistant_terms_acceptance.name "{{ id }}"
 * ```
 */
export class TermsAcceptance extends pulumi.CustomResource {
    /**
     * Get an existing TermsAcceptance resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TermsAcceptanceState, opts?: pulumi.CustomResourceOptions): TermsAcceptance {
        return new TermsAcceptance(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'grafana:assistant/termsAcceptance:TermsAcceptance';

    /**
     * Returns true if the given object is an instance of TermsAcceptance.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TermsAcceptance {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TermsAcceptance.__pulumiType;
    }

    /**
     * Whether the current Grafana Assistant terms are accepted for the stack. Setting this to `false` withdraws acceptance.
     */
    declare public readonly accepted: pulumi.Output<boolean>;

    /**
     * Create a TermsAcceptance resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TermsAcceptanceArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TermsAcceptanceArgs | TermsAcceptanceState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TermsAcceptanceState | undefined;
            resourceInputs["accepted"] = state?.accepted;
        } else {
            const args = argsOrState as TermsAcceptanceArgs | undefined;
            if (args?.accepted === undefined && !opts.urn) {
                throw new Error("Missing required property 'accepted'");
            }
            resourceInputs["accepted"] = args?.accepted;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TermsAcceptance.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TermsAcceptance resources.
 */
export interface TermsAcceptanceState {
    /**
     * Whether the current Grafana Assistant terms are accepted for the stack. Setting this to `false` withdraws acceptance.
     */
    accepted?: pulumi.Input<boolean>;
}

/**
 * The set of arguments for constructing a TermsAcceptance resource.
 */
export interface TermsAcceptanceArgs {
    /**
     * Whether the current Grafana Assistant terms are accepted for the stack. Setting this to `false` withdraws acceptance.
     */
    accepted: pulumi.Input<boolean>;
}
