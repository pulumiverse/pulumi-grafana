// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages Grafana Git Sync repositories for provisioning dashboards and related resources.
 *
 * ## Example Usage
 *
 * ### GitHub Repository with Token Authentication
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const example = new grafana.AppsProvisioningRepositoryV0alpha1("example", {
 *     metadata: {
 *         uid: "my-github-folder-repo",
 *     },
 *     spec: {
 *         title: "My GitHub Folder Repository",
 *         description: "Folder-scoped GitHub repository authenticated directly with a token",
 *         type: "github",
 *         workflows: [
 *             "write",
 *             "branch",
 *         ],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         github: {
 *             url: "https://github.com/example/grafana-dashboards",
 *             branch: "main",
 *             path: "grafanatftest",
 *         },
 *     },
 *     secure: {
 *         token: {
 *             create: "replace-me",
 *         },
 *     },
 *     secureVersion: 1,
 * });
 * ```
 *
 * ### GitHub Repository via GitHub App Connection
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const githubApp = new grafana.AppsProvisioningConnectionV0alpha1("github_app", {
 *     metadata: {
 *         uid: "my-github-app-connection",
 *     },
 *     spec: {
 *         title: "My GitHub App Connection",
 *         description: "GitHub App connection used by a folder-scoped Git Sync repository",
 *         type: "github",
 *         url: "https://github.com",
 *         github: {
 *             appId: "12345",
 *             installationId: "67890",
 *         },
 *     },
 *     secure: {
 *         privateKey: {
 *             create: "replace-me",
 *         },
 *     },
 *     secureVersion: 1,
 * });
 * const githubAppAppsProvisioningRepositoryV0alpha1 = new grafana.AppsProvisioningRepositoryV0alpha1("github_app", {
 *     metadata: {
 *         uid: "my-github-app-folder-repo",
 *     },
 *     spec: {
 *         title: "My GitHub App Folder Repository",
 *         description: "Folder-scoped GitHub repository authenticated via a referenced GitHub App connection",
 *         type: "github",
 *         workflows: [
 *             "write",
 *             "branch",
 *         ],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         github: {
 *             url: "https://github.com/example/grafana-dashboards",
 *             branch: "main",
 *             path: "grafanatftest",
 *         },
 *         connection: {
 *             name: githubApp.metadata.apply(metadata => metadata?.uid),
 *         },
 *     },
 * }, {
 *     dependsOn: [githubApp],
 * });
 * ```
 *
 * ### Bitbucket Repository with Token Authentication - Enterprise/Cloud only
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const bitbucketToken = new grafana.AppsProvisioningRepositoryV0alpha1("bitbucket_token", {
 *     metadata: {
 *         uid: "my-bitbucket-folder-repo",
 *     },
 *     spec: {
 *         title: "My Bitbucket Folder Repository",
 *         description: "Folder-scoped Bitbucket repository authenticated directly with an Atlassian API token",
 *         type: "bitbucket",
 *         workflows: [
 *             "write",
 *             "branch",
 *         ],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         bitbucket: {
 *             url: "https://bitbucket.org/example/grafana-dashboards",
 *             branch: "main",
 *             path: "grafanatftest",
 *             tokenUser: "x-bitbucket-api-token-auth",
 *         },
 *     },
 *     secure: {
 *         token: {
 *             create: "replace-me",
 *         },
 *     },
 *     secureVersion: 1,
 * });
 * ```
 *
 * ### GitLab Repository with Token Authentication - Enterprise/Cloud only
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const gitlabToken = new grafana.AppsProvisioningRepositoryV0alpha1("gitlab_token", {
 *     metadata: {
 *         uid: "my-gitlab-folder-repo",
 *     },
 *     spec: {
 *         title: "My GitLab Folder Repository",
 *         description: "Folder-scoped GitLab repository authenticated directly with a token",
 *         type: "gitlab",
 *         workflows: [
 *             "write",
 *             "branch",
 *         ],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         gitlab: {
 *             url: "https://gitlab.com/example/grafana-dashboards",
 *             branch: "main",
 *             path: "grafanatftest",
 *         },
 *     },
 *     secure: {
 *         token: {
 *             create: "replace-me",
 *         },
 *     },
 *     secureVersion: 1,
 * });
 * ```
 *
 * ### Generic Git Repository
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const pureGit = new grafana.AppsProvisioningRepositoryV0alpha1("pure_git", {
 *     metadata: {
 *         uid: "my-pure-git-folder-repo",
 *     },
 *     spec: {
 *         title: "My Pure Git Folder Repository",
 *         description: "Folder-scoped generic Git repository authenticated with a token",
 *         type: "git",
 *         workflows: ["write"],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         git: {
 *             url: "https://git.example.com/platform/dashboards.git",
 *             branch: "main",
 *             path: "grafanatftest",
 *             tokenUser: "git",
 *         },
 *     },
 *     secure: {
 *         token: {
 *             create: "replace-me",
 *         },
 *     },
 *     secureVersion: 1,
 * });
 * ```
 *
 * ### Local Filesystem Repository
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const localRepo = new grafana.AppsProvisioningRepositoryV0alpha1("local_repo", {
 *     metadata: {
 *         uid: "my-local-folder-repo",
 *     },
 *     spec: {
 *         title: "My Local Folder Repository",
 *         description: "Folder-scoped local filesystem repository",
 *         type: "local",
 *         workflows: ["write"],
 *         sync: {
 *             enabled: true,
 *             target: "folder",
 *             intervalSeconds: 60,
 *         },
 *         local: {
 *             path: "/usr/share/grafana/conf/provisioning/my-local-repo",
 *         },
 *     },
 * });
 * ```
 */
export class AppsProvisioningRepositoryV0alpha1 extends pulumi.CustomResource {
    /**
     * Get an existing AppsProvisioningRepositoryV0alpha1 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AppsProvisioningRepositoryV0alpha1State, opts?: pulumi.CustomResourceOptions): AppsProvisioningRepositoryV0alpha1 {
        return new AppsProvisioningRepositoryV0alpha1(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'grafana:index/appsProvisioningRepositoryV0alpha1:AppsProvisioningRepositoryV0alpha1';

    /**
     * Returns true if the given object is an instance of AppsProvisioningRepositoryV0alpha1.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AppsProvisioningRepositoryV0alpha1 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AppsProvisioningRepositoryV0alpha1.__pulumiType;
    }

    /**
     * The metadata of the resource.
     */
    declare public readonly metadata: pulumi.Output<outputs.AppsProvisioningRepositoryV0alpha1Metadata | undefined>;
    /**
     * Options for applying the resource.
     */
    declare public readonly options: pulumi.Output<outputs.AppsProvisioningRepositoryV0alpha1Options | undefined>;
    /**
     * Sensitive credentials. Values are write-only and never stored in Terraform state.
     */
    declare public readonly secure: pulumi.Output<outputs.AppsProvisioningRepositoryV0alpha1Secure | undefined>;
    /**
     * Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
     */
    declare public readonly secureVersion: pulumi.Output<number | undefined>;
    /**
     * The spec of the resource.
     */
    declare public readonly spec: pulumi.Output<outputs.AppsProvisioningRepositoryV0alpha1Spec | undefined>;

    /**
     * Create a AppsProvisioningRepositoryV0alpha1 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: AppsProvisioningRepositoryV0alpha1Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AppsProvisioningRepositoryV0alpha1Args | AppsProvisioningRepositoryV0alpha1State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AppsProvisioningRepositoryV0alpha1State | undefined;
            resourceInputs["metadata"] = state?.metadata;
            resourceInputs["options"] = state?.options;
            resourceInputs["secure"] = state?.secure;
            resourceInputs["secureVersion"] = state?.secureVersion;
            resourceInputs["spec"] = state?.spec;
        } else {
            const args = argsOrState as AppsProvisioningRepositoryV0alpha1Args | undefined;
            resourceInputs["metadata"] = args?.metadata;
            resourceInputs["options"] = args?.options;
            resourceInputs["secure"] = args?.secure;
            resourceInputs["secureVersion"] = args?.secureVersion;
            resourceInputs["spec"] = args?.spec;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AppsProvisioningRepositoryV0alpha1.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AppsProvisioningRepositoryV0alpha1 resources.
 */
export interface AppsProvisioningRepositoryV0alpha1State {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Metadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Options>;
    /**
     * Sensitive credentials. Values are write-only and never stored in Terraform state.
     */
    secure?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Secure>;
    /**
     * Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
     */
    secureVersion?: pulumi.Input<number>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Spec>;
}

/**
 * The set of arguments for constructing a AppsProvisioningRepositoryV0alpha1 resource.
 */
export interface AppsProvisioningRepositoryV0alpha1Args {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Metadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Options>;
    /**
     * Sensitive credentials. Values are write-only and never stored in Terraform state.
     */
    secure?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Secure>;
    /**
     * Set this to 1 when using `secure`, then increment it to trigger re-application of secure values.
     */
    secureVersion?: pulumi.Input<number>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.AppsProvisioningRepositoryV0alpha1Spec>;
}
