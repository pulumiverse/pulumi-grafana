// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages services in Grafana Service Center via the Service Model API (`servicemodel.ext.grafana.com/v1alpha1`, kind `Component`).
 *
 * Services are catalog components of type `service` (the default for `spec.type`). Service Center currently
 * displays components of type `service`; components of other types are stored but not shown.
 *
 * **Availability**: Grafana Cloud. The API is **v1alpha1** and may evolve; the attributes exposed here are stable.
 *
 * **Naming**: `metadata.uid` is the object name, e.g. `checkout-service`. It can only contain lowercase
 * letters, numbers and dashes, must start and end with a letter or number, and must be 2 to 63 characters long.
 * Changing `metadata.uid` replaces the resource (destroy and create). The uid is also the service identifier:
 * dashboards, alerts, SLOs and other resources are matched to the service when they carry a `serviceName` label
 * or tag equal to it; the `identifiers` block can add further values to match.
 *
 * **Backstage catalog sync**: services [imported from Backstage](https://grafana.com/docs/grafana-cloud/alerting-and-irm/service-center/create-a-service/)
 * must be managed there, not in Terraform; otherwise the two will repeatedly overwrite each other.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * // A minimal service.
 * const payments = new grafana.AppsServicemodelComponentV1alpha1("payments", {
 *     metadata: {
 *         uid: "payments-api",
 *     },
 *     spec: {
 *         title: "Payments API",
 *     },
 * });
 * const checkout = new grafana.oss.Team("checkout", {name: "Checkout Team"});
 * // A service with ownership, dependencies and links.
 * const checkoutAppsServicemodelComponentV1alpha1 = new grafana.AppsServicemodelComponentV1alpha1("checkout", {
 *     metadata: {
 *         uid: "checkout-service",
 *     },
 *     spec: {
 *         title: "Checkout Service",
 *         description: "Handles checkout and payment orchestration.",
 *         identifiers: [
 *             {
 *                 key: "service_name",
 *                 value: "Checkout_Service",
 *             },
 *             {
 *                 key: "namespace",
 *                 value: "checkout-prod",
 *             },
 *         ],
 *         ownerRef: {
 *             name: checkout.teamUid,
 *         },
 *         dependsOnRefs: [{
 *             name: payments.metadata.apply(metadata => metadata?.uid),
 *         }],
 *         links: [{
 *             url: "https://github.com/example/checkout",
 *             title: "Source code",
 *             type: "repository",
 *         }],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * !/bin/bash
 * Import an existing Service Center component by its UID (the Kubernetes object name).
 *
 * ```sh
 * $ pulumi import grafana:index/appsServicemodelComponentV1alpha1:AppsServicemodelComponentV1alpha1 checkout checkout-service
 * ```
 */
export class AppsServicemodelComponentV1alpha1 extends pulumi.CustomResource {
    /**
     * Get an existing AppsServicemodelComponentV1alpha1 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AppsServicemodelComponentV1alpha1State, opts?: pulumi.CustomResourceOptions): AppsServicemodelComponentV1alpha1 {
        return new AppsServicemodelComponentV1alpha1(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'grafana:index/appsServicemodelComponentV1alpha1:AppsServicemodelComponentV1alpha1';

    /**
     * Returns true if the given object is an instance of AppsServicemodelComponentV1alpha1.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AppsServicemodelComponentV1alpha1 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AppsServicemodelComponentV1alpha1.__pulumiType;
    }

    /**
     * The metadata of the resource.
     */
    declare public readonly metadata: pulumi.Output<outputs.AppsServicemodelComponentV1alpha1Metadata | undefined>;
    /**
     * Options for applying the resource.
     */
    declare public readonly options: pulumi.Output<outputs.AppsServicemodelComponentV1alpha1Options | undefined>;
    /**
     * The spec of the resource.
     */
    declare public readonly spec: pulumi.Output<outputs.AppsServicemodelComponentV1alpha1Spec | undefined>;

    /**
     * Create a AppsServicemodelComponentV1alpha1 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: AppsServicemodelComponentV1alpha1Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AppsServicemodelComponentV1alpha1Args | AppsServicemodelComponentV1alpha1State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AppsServicemodelComponentV1alpha1State | undefined;
            resourceInputs["metadata"] = state?.metadata;
            resourceInputs["options"] = state?.options;
            resourceInputs["spec"] = state?.spec;
        } else {
            const args = argsOrState as AppsServicemodelComponentV1alpha1Args | undefined;
            resourceInputs["metadata"] = args?.metadata;
            resourceInputs["options"] = args?.options;
            resourceInputs["spec"] = args?.spec;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AppsServicemodelComponentV1alpha1.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AppsServicemodelComponentV1alpha1 resources.
 */
export interface AppsServicemodelComponentV1alpha1State {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Metadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Options>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Spec>;
}

/**
 * The set of arguments for constructing a AppsServicemodelComponentV1alpha1 resource.
 */
export interface AppsServicemodelComponentV1alpha1Args {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Metadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Options>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.AppsServicemodelComponentV1alpha1Spec>;
}
