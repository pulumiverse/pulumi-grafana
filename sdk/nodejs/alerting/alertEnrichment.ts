// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Manages [Grafana Cloud Alert Enrichment](https://grafana.com/docs/grafana-cloud/alerting-and-irm/alerting/configure-notifications/alert-enrichment/).
 *
 * Alert enrichment is currently in private preview. Grafana Labs offers support on a best-effort basis, and breaking changes might occur prior to the feature being made generally available
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as grafana from "@pulumiverse/grafana";
 *
 * const enrichment = new grafana.alerting.AlertEnrichment("enrichment", {
 *     metadata: {
 *         uid: "test_enrichment",
 *     },
 *     spec: {
 *         title: "Comprehensive alert enrichment",
 *         description: "Demonstrates many enrichment steps and configurations",
 *         alertRuleUids: [
 *             "alert-rule-1",
 *             "alert-rule-2",
 *         ],
 *         receivers: [
 *             "webhook",
 *             "slack-critical",
 *         ],
 *         labelMatchers: [
 *             {
 *                 type: "=",
 *                 name: "severity",
 *                 value: "critical",
 *             },
 *             {
 *                 type: "=~",
 *                 name: "team",
 *                 value: "alerting|alerting-team",
 *             },
 *         ],
 *         annotationMatchers: [{
 *             type: "!=",
 *             name: "runbook_url",
 *             value: "",
 *         }],
 *         steps: [
 *             {
 *                 assign: {
 *                     timeout: "30s",
 *                     annotations: {
 *                         priority: "high",
 *                         runbook_url: "https://runbooks.grafana.com/alert-handling",
 *                     },
 *                 },
 *             },
 *             {
 *                 external: {
 *                     url: "https://some-api.grafana.com/alert-enrichment",
 *                 },
 *             },
 *             {
 *                 dataSource: {
 *                     timeout: "30s",
 *                     logsQuery: {
 *                         dataSourceType: "loki",
 *                         dataSourceUid: "loki-uid-123",
 *                         expr: "{job=\"my-app\"} |= \"error\"",
 *                         maxLines: 5,
 *                     },
 *                 },
 *             },
 *             {
 *                 dataSource: {
 *                     timeout: "30s",
 *                     rawQuery: {
 *                         refId: "A",
 *                         request: JSON.stringify({
 *                             datasource: {
 *                                 type: "prometheus",
 *                                 uid: "prometheus-uid-456",
 *                             },
 *                             expr: "rate(http_requests_total[5m])",
 *                             refId: "A",
 *                             intervalMs: 1000,
 *                             maxDataPoints: 43200,
 *                         }),
 *                     },
 *                 },
 *             },
 *             {
 *                 sift: {},
 *             },
 *             {
 *                 explain: {
 *                     annotation: "ai_explanation",
 *                 },
 *             },
 *             {
 *                 assistantInvestigations: {},
 *             },
 *             {
 *                 conditional: {
 *                     "if": {
 *                         labelMatchers: [{
 *                             type: "=",
 *                             name: "severity",
 *                             value: "critical",
 *                         }],
 *                     },
 *                     then: {
 *                         steps: [
 *                             {
 *                                 assign: {
 *                                     annotations: {
 *                                         escalation_level: "immediate",
 *                                     },
 *                                 },
 *                             },
 *                             {
 *                                 external: {
 *                                     url: "https://irm.grafana.com/create-incident",
 *                                 },
 *                             },
 *                         ],
 *                     },
 *                     "else": {
 *                         steps: [{
 *                             assign: {
 *                                 annotations: {
 *                                     escalation_level: "standard",
 *                                 },
 *                             },
 *                         }],
 *                     },
 *                 },
 *             },
 *         ],
 *     },
 * });
 * ```
 */
export class AlertEnrichment extends pulumi.CustomResource {
    /**
     * Get an existing AlertEnrichment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertEnrichmentState, opts?: pulumi.CustomResourceOptions): AlertEnrichment {
        return new AlertEnrichment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'grafana:alerting/alertEnrichment:AlertEnrichment';

    /**
     * Returns true if the given object is an instance of AlertEnrichment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AlertEnrichment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AlertEnrichment.__pulumiType;
    }

    /**
     * The metadata of the resource.
     */
    declare public readonly metadata: pulumi.Output<outputs.alerting.AlertEnrichmentMetadata | undefined>;
    /**
     * Options for applying the resource.
     */
    declare public readonly options: pulumi.Output<outputs.alerting.AlertEnrichmentOptions | undefined>;
    /**
     * The spec of the resource.
     */
    declare public readonly spec: pulumi.Output<outputs.alerting.AlertEnrichmentSpec | undefined>;

    /**
     * Create a AlertEnrichment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: AlertEnrichmentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertEnrichmentArgs | AlertEnrichmentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertEnrichmentState | undefined;
            resourceInputs["metadata"] = state?.metadata;
            resourceInputs["options"] = state?.options;
            resourceInputs["spec"] = state?.spec;
        } else {
            const args = argsOrState as AlertEnrichmentArgs | undefined;
            resourceInputs["metadata"] = args?.metadata;
            resourceInputs["options"] = args?.options;
            resourceInputs["spec"] = args?.spec;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AlertEnrichment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AlertEnrichment resources.
 */
export interface AlertEnrichmentState {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.alerting.AlertEnrichmentMetadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.alerting.AlertEnrichmentOptions>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.alerting.AlertEnrichmentSpec>;
}

/**
 * The set of arguments for constructing a AlertEnrichment resource.
 */
export interface AlertEnrichmentArgs {
    /**
     * The metadata of the resource.
     */
    metadata?: pulumi.Input<inputs.alerting.AlertEnrichmentMetadata>;
    /**
     * Options for applying the resource.
     */
    options?: pulumi.Input<inputs.alerting.AlertEnrichmentOptions>;
    /**
     * The spec of the resource.
     */
    spec?: pulumi.Input<inputs.alerting.AlertEnrichmentSpec>;
}
